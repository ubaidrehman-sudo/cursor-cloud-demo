DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tasks_db?schema=public"
PORT=3000
# Optional comma-separated list. If omitted, the API reflects the request Origin (works for local Docker + any dev port).
# CORS_ORIGIN=http://localhost:3001
