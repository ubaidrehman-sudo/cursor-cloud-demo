DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tasks_db?schema=public"
PORT=3000
# Allowed browser origin when CORS_ORIGIN is not set (matches docker-compose defaults).
# FRONTEND_ORIGIN=http://localhost:3001
# Optional comma-separated list; when set, overrides FRONTEND_ORIGIN.
# CORS_ORIGIN=http://localhost:3001,http://localhost:3000
